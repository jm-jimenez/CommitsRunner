name: Commits

on:
  pull_request:
    branches: ["master"]
    types: [opened, reopened, edited]
  push:
    branches: ["master"]

jobs:
  use_share:
    steps:
      - name: hey
        uses: ./.github/workflows/shared.yml
